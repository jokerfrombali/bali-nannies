# Sanur Nannies — site

Static site (EN at `/`, RU at `/ru/`). Generated by `site_build.py` into `site/`; deployed to GitHub Pages by `.github/workflows/pages.yml`.

```bash
python site_build.py
```

Before launch fill in `WA`, `EMAIL`, `DOMAIN` in `site_build.py`, real prices and photos.
